# Credentials
.env

# Runtime data (price history DB — local only, not committed)
data/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
*.egg-info/

# pytest / coverage
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# mypy / ruff caches
.mypy_cache/
.ruff_cache/

# Terraform — local state and secrets (never commit)
deploy/terraform/.terraform/
deploy/terraform/terraform.tfstate
deploy/terraform/terraform.tfstate*.backup
deploy/terraform/terraform.tfvars
deploy/terraform/*.tfplan

